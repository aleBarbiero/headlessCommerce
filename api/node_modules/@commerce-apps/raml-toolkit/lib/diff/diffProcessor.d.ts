import { NodeDiff } from "./jsonDiff";
export declare const defaultRulesPath: string;
export declare const defaultRulesPackagePath: string;
/**
 * Generate differences between two RAML files and apply rules
 * @param leftRaml - Base RAML file to compare
 * @param rightRaml - Other RAML file to compare with the left RAML to find differences
 * @param rulesPath - Optional rules file to apply on the differences
 *
 * @returns Changes to the APIs
 */
export declare function findApiChanges(leftRaml: string, rightRaml: string, rulesPath?: string): Promise<NodeDiff[]>;
/**
 * Generate differences between two RAML files
 * @param leftRaml - Base RAML file to compare
 * @param rightRaml - Other RAML file to compare with the left RAML to find differences
 *
 * @returns Array of NodeDiff objects
 */
export declare function diffRaml(leftRaml: string, rightRaml: string): Promise<NodeDiff[]>;
export declare type RamlDiff = {
    file: string;
    message?: string;
    diff?: NodeDiff[];
};
//# sourceMappingURL=diffProcessor.d.ts.map