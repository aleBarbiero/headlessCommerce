import { Command, flags } from "@oclif/command";
import { OutputFlags } from "@oclif/parser";
export declare class DiffCommand extends Command {
    static description: string;
    static flags: {
        version: import("@oclif/parser/lib/flags").IBooleanFlag<void>;
        help: import("@oclif/parser/lib/flags").IBooleanFlag<void>;
        ruleset: flags.IOptionFlag<string>;
        "diff-only": import("@oclif/parser/lib/flags").IBooleanFlag<boolean>;
        dir: import("@oclif/parser/lib/flags").IBooleanFlag<boolean>;
        "out-file": flags.IOptionFlag<string>;
    };
    static args: {
        name: string;
        required: boolean;
        description: string;
    }[];
    /**
     * If a file is given, saves data to the file. Otherwise, just logs the data.
     * The data must be JSON-serializable.
     *
     * @param json - The data to save or log
     * @param file - The file to save to
     */
    protected _saveOrLog(json: unknown, file?: string): Promise<void>;
    /**
     * Find the differences between two directories containing API spec files.
     * Only finds differences, does not classify using a ruleset.
     *
     * @param baseApis - Path to an API config file in the base directory
     * @param newApis - Path to an API config file in the new directory
     * @param flags - Parsed CLI flags passed to the command
     */
    protected _diffDirs(baseApis: string, newApis: string, flags: OutputFlags<typeof DiffCommand.flags>): Promise<void>;
    /**
     * Find the differences between two API specification files. Does not classify
     * the differences.
     *
     * @param baseApis - Path to a base API spec file
     * @param newApis - Path to a new API spec file
     * @param flags - Parsed CLI flags passed to the command
     */
    protected _diffFiles(baseApis: string, newApis: string, flags: OutputFlags<typeof DiffCommand.flags>): Promise<void>;
    /**
     * Find the differences between two API specification files and classifies
     * the changes according to a ruleset.
     *
     * @param baseApis - Path to a base API spec file
     * @param newApis - Path to a new API spec file
     * @param flags - Parsed CLI flags passed to the command
     */
    protected _diffFilesUsingRuleset(baseApis: string, newApis: string, flags: OutputFlags<typeof DiffCommand.flags>): Promise<void>;
    run(): Promise<void>;
}
//# sourceMappingURL=index.d.ts.map